{% extends 'base.html.twig' %}

{% block title %}Organization
{% endblock %}

{% block body %}
	<h1>Organisations</h1>
	<table class="table">
		<thead>
			<tr>
				<th scope="col">
					Name
				</th>
				<th scope="col">
					Description
				</th>
			</tr>
		</thead>
		<tbody>
			{% for data in datas %}
				<tr>
					<td>{{data.name}}</td>
					<td>{{data.description}}</td>
				</tr>
				{% else %}No data found
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
